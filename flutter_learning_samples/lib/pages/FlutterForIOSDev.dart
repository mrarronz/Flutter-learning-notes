import 'package:flutter/material.dart';

class FlutterForIOSDev extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(title: new Text('Flutter For iOS Dev')),
      body: new Center(
        child: new Text('iOS Dev'),
      ),
    );
  }
}
